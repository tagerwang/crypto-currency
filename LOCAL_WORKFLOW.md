# 本地 Kiro + 远程服务器工作流程

## 🎯 推荐工作方式

你在**本地 Mac** 使用 Kiro 和 Claude 开发，服务器只负责运行服务。

```
┌──────────────────────────────┐
│   你的 Mac（本地开发环境）    │
│                              │
│  ✅ Kiro IDE                 │
│  ✅ Claude Sonnet 4.5        │
│  ✅ 代码编辑和调试            │
│  ✅ Git 版本控制             │
│  ✅ 本地测试                 │
└──────────────┬───────────────┘
               │
               │ SSH/SCP 部署
               │
┌──────────────▼───────────────┐
│   Vultr 服务器（生产环境）    │
│                              │
│  🚀 运行 MCP API 服务        │
│  🌐 对外提供 HTTP API        │
│  📊 处理实际请求             │
│  ⚡ 24/7 运行               │
└──────────────────────────────┘
```

## 📝 日常工作流程

### 1️⃣ 在本地 Kiro 中开发

```bash
# 在 Kiro 终端中
cd ~/exercise/crypto-currency-main

# 使用 Claude 帮你修改代码
# 例如："帮我优化 binance_mcp 模块的错误处理"
# 或者："添加一个新的 API 端点获取交易量排行"
```

### 2️⃣ 本地测试

```bash
# 在 Kiro 终端中本地运行测试
python3 mcp_http_server.py

# 在另一个终端测试
curl http://localhost:8080/health
```

### 3️⃣ 部署到服务器

**方法 A：使用管理脚本（推荐）**
```bash
# 在 Kiro 终端中
chmod +x server_manager.sh

# 编辑脚本，设置你的服务器 IP
# SERVER_IP="YOUR_SERVER_IP"

# 运行管理工具
./server_manager.sh

# 选择 1 - 部署代码
```

**方法 B：手动部署**
```bash
# 在 Kiro 终端中
scp -r ./* root@YOUR_SERVER_IP:/opt/mcp-crypto-api/
ssh root@YOUR_SERVER_IP "supervisorctl restart mcp-crypto-api"
```

**方法 C：使用 Git（最专业）**
```bash
# 1. 在本地提交代码
git add .
git commit -m "更新功能"
git push

# 2. 在服务器上拉取
ssh root@YOUR_SERVER_IP
cd /opt/mcp-crypto-api
git pull
supervisorctl restart mcp-crypto-api
```

### 4️⃣ 在本地监控服务器

```bash
# 在 Kiro 终端中查看服务器日志
ssh root@YOUR_SERVER_IP "tail -f /var/log/mcp-crypto-api.out.log"

# 查看服务状态
ssh root@YOUR_SERVER_IP "supervisorctl status"

# 测试 API
curl http://YOUR_SERVER_IP/health
```

## 🛠️ 常用命令速查

### 在本地 Kiro 终端执行

```bash
# 快速部署
./server_manager.sh  # 选择 1

# 查看服务器日志
ssh root@YOUR_SERVER_IP "tail -100 /var/log/mcp-crypto-api.out.log"

# 重启服务
ssh root@YOUR_SERVER_IP "supervisorctl restart mcp-crypto-api"

# 测试 API
curl http://YOUR_SERVER_IP/binance/spot/price?symbol=BTC

# 连接到服务器
ssh root@YOUR_SERVER_IP
```

## 💡 使用 Claude 的技巧

在 Kiro 中，你可以直接问 Claude：

### 开发相关
- "帮我添加一个获取 24 小时交易量排行的 API"
- "优化 comprehensive_analysis 函数的性能"
- "添加错误重试机制"
- "帮我写一个缓存层减少 API 调用"

### 部署相关
- "帮我写一个自动部署脚本"
- "如何配置 HTTPS？"
- "添加 API 访问限流"
- "如何监控服务器性能？"

### 调试相关
- "为什么 API 返回 500 错误？"
- "如何查看详细的错误日志？"
- "优化内存使用"

## 🔧 配置 SSH 免密登录（可选）

让部署更方便：

```bash
# 在本地 Kiro 终端执行
# 1. 生成 SSH 密钥（如果还没有）
ssh-keygen -t rsa -b 4096

# 2. 复制公钥到服务器
ssh-copy-id root@YOUR_SERVER_IP

# 3. 现在可以免密登录了
ssh root@YOUR_SERVER_IP
```

## 📊 监控和管理

### 在本地创建快捷命令

在 Kiro 终端中，你可以创建别名：

```bash
# 编辑 ~/.zshrc
nano ~/.zshrc

# 添加以下内容（替换 YOUR_SERVER_IP）
alias mcp-deploy="scp -r ./* root@YOUR_SERVER_IP:/opt/mcp-crypto-api/ && ssh root@YOUR_SERVER_IP 'supervisorctl restart mcp-crypto-api'"
alias mcp-logs="ssh root@YOUR_SERVER_IP 'tail -f /var/log/mcp-crypto-api.out.log'"
alias mcp-status="ssh root@YOUR_SERVER_IP 'supervisorctl status mcp-crypto-api'"
alias mcp-ssh="ssh root@YOUR_SERVER_IP"

# 保存后重新加载
source ~/.zshrc

# 现在可以使用简短命令
mcp-deploy   # 部署
mcp-logs     # 查看日志
mcp-status   # 查看状态
mcp-ssh      # 连接服务器
```

## 🎨 Kiro 集成示例

### 创建 Kiro Hook（自动部署）

你可以在 Kiro 中创建一个 Hook，每次保存文件时自动部署：

1. 在 Kiro 中打开命令面板（Cmd+Shift+P）
2. 搜索 "Open Kiro Hook UI"
3. 创建新 Hook：
   - 触发器：文件保存
   - 动作：运行 `./server_manager.sh` 选项 1

### 使用 Kiro 的终端

Kiro 内置终端完全支持 SSH 和所有命令：

```bash
# 在 Kiro 终端中（Cmd+`）
ssh root@YOUR_SERVER_IP

# 或者使用管理脚本
./server_manager.sh
```

## 🚀 高级工作流

### 使用 Git 自动化部署

```bash
# 1. 在服务器上设置 Git Hook
ssh root@YOUR_SERVER_IP
cd /opt/mcp-crypto-api
cat > .git/hooks/post-merge << 'EOF'
#!/bin/bash
supervisorctl restart mcp-crypto-api
EOF
chmod +x .git/hooks/post-merge

# 2. 在本地推送代码
git push

# 3. 在服务器上自动拉取和重启
ssh root@YOUR_SERVER_IP "cd /opt/mcp-crypto-api && git pull"
```

## ❓ 常见问题

### Q: 我必须在服务器上安装 Kiro 吗？
**A:** 不需要！服务器只运行你的 Python 代码，不需要 IDE。

### Q: 如何在本地调试服务器上的问题？
**A:** 
1. 通过 SSH 查看日志
2. 或者将服务器日志下载到本地
3. 在本地复现问题并用 Claude 帮你解决

### Q: 服务器需要什么配置？
**A:** 
- 最小：1 vCPU, 2GB RAM（约 $10/月）
- 推荐：2 vCPU, 4GB RAM（约 $20/月）
- 无需图形界面

### Q: 如何保证服务稳定运行？
**A:** 
- Supervisor 会自动重启崩溃的服务
- Nginx 提供反向代理和负载均衡
- 定期查看日志监控异常

## 📚 总结

**最佳实践：**
1. ✅ 本地 Mac 用 Kiro + Claude 开发
2. ✅ 服务器用终端版 Ubuntu 运行服务
3. ✅ 通过 SSH 管理和部署
4. ✅ 使用 Git 进行版本控制
5. ✅ 用管理脚本简化操作

**不要：**
- ❌ 在服务器上安装图形界面
- ❌ 在服务器上安装 IDE
- ❌ 直接在服务器上编辑代码

这样既能享受 Kiro 和 Claude 的强大功能，又能保持服务器的高效运行！
